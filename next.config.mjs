/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/20260214_myportfolio',
    images: {
        unoptimized: true,
    },
    swcMinify: false,
};

export default nextConfig;
